import styled from 'styled-components'

import { IOrder } from 'src/types'

interface Props {
  orders: IOrder[]
}

export const OrdersProfile = ({ orders }: Props) => {
  return <Wrap></Wrap>
}

const Wrap = styled.div``
